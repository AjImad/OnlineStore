import React from 'react';
import { Box } from '@mui/system';

const wishlist = () => {
    return (
        <Box
            sx={{
                width: '100%',
                position: 'relative',
                mt: '65px'
            }}
        >
            This is wishlist
        </Box>
    );
}

export default wishlist;