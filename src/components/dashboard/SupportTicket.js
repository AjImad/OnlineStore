import { Box } from '@mui/material';
import React from 'react';

const SupportTicket = () => {
    return (
        <Box
            sx={{
                width: '100%',
                position: 'relative',
                mt: { xs: '50px', md: '65px' }
            }}
        >
            This is Support page
        </Box>
    );
}

export default SupportTicket;